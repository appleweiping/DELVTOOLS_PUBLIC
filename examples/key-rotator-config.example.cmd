@echo off
rem Copy this file to key-rotator.local.cmd in DEVTOOLS_ROOT and keep that copy ignored.

rem set "KEY_ROTATOR_SCRIPT=<script-path>"
rem set "NODE_EXE=<node-executable-path>"
rem set "ROTATOR_PORT=<local-port>"
rem set "ROTATOR_PROXY_TOKEN=<independent-random-local-value>"
rem set "ROTATOR_KEYS=<local-values>"
rem set "ROTATOR_TARGETS=<provider-endpoints>"
